defaults:
  - _self_
  - paths: default
  - wandb: default
  - hydra: default
  - experiment: null


model: resnet18
dataset: path

epochs: 100
batch_size: 128
iter_size: 1
learning_rate: 0.001
gamma: 0.1

checkpoint: null